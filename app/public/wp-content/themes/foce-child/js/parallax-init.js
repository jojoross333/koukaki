document.addEventListener('DOMContentLoaded', function() {
    // Initialiser Skrollr
    var s = skrollr.init();

    // Vérifier si Skrollr est bien initialisé
    console.log("Skrollr initialized successfully!");
});
